# 「お問い合わせフォーム・管理画面・公開API」開発完全ロードマップ

## 【フェーズ1：初期設定とデータベース（基礎）】

### ステップ1：環境構築とSail起動
`sail up -d` までの環境構築と起動処理を実行。

### ステップ2：データベースの設計とマイグレーション
仕様通りに `users`, `categories`, `contacts`, `tags`, `contact_tag` テーブルを作成。

### ステップ3：シーダーの実装と初期データ投入
* `sail artisan db:seed` による初期データ投入。
* テストユーザー、固定カテゴリ（5件）、固定タグ（5件）の作成。
* Fakerによるダミーお問い合わせ（20件、タグ紐付け含む）の投入。

---

## 【フェーズ2：公開お問い合わせフォーム（画面定義：PG01〜PG03）】

### ステップ4：入力画面（PG01）の表示とデータ連携
* **ルーティング**: `GET /`
* `ContactController@index` でカテゴリとタグの全データを取得し、Bladeへ渡してドロップダウン・チェックボックスに表示。

### ステップ5：【重要】確認画面への遷移とバリデーション
* **ルーティング**: `POST /contacts/confirm`
* フォームリクエストを用いて要件（a〜j）のエラーメッセージを一字一句違わず定義し、バリデーション通過時に確認ページ（PG02）へ遷移。

### ステップ6：確認画面（PG02）の表示とデータ保持
* 入力内容（カテゴリ名・タグ名を含む）を文字列として正しく表示。
* 「修正」で戻る際に `old()` 等でデータを保持する実装。

### ステップ7：送信処理とサンクスページ（PG03）
* **ルーティング**: `POST /contacts` ＆ `GET /thanks`
* `contacts` テーブルへ保存、中間テーブル `contact_tag` へタグを記録し、送信完了画面へリダイレクト。

---

## 【フェーズ3：管理機能とアクセス制御（画面定義：PG05〜PG06）】

### ステップ8：管理者登録・ログイン機能の連携（Fortify）
* `/register` および `/login` のビュー・バリデーション文言の設定。
* 管理画面ルートへの `auth` ミドルウェアによるアクセス制限。

### ステップ9：管理画面（PG05）の一覧表示と検索・リセット
* **ルーティング**: `GET /admin`
* `AdminController@index` で名前（姓・名部分一致）、メール、性別、カテゴリ、日付の検索ロジックを実装。
* 7件ごとのページネーションとEager Loading（N+1問題対策）の徹底。

### ステップ10：詳細ページ（PG05-2）とデータ削除
* **ルーティング**: `GET /admin/contacts/{contact}` 等
* 詳細情報の文字列表示（画面遷移）と、`DELETE` リクエストによるレコード削除（中間テーブルの自動削除）。

### ステップ11：タグマスタ管理とタグ編集（PG05-3）
* タグの追加（名前必須・50文字以内・ユニーク）。
* 編集画面への遷移、更新（`PUT`）、削除（`DELETE`）。

### ステップ12：【応用】CSVエクスポート機能
* **ルーティング**: `GET /contacts/export`
* 検索条件に一致するデータを仕様書通りの列順（ID/氏名/性別文字列/...）で、BOM付きCSVとして新着順でダウンロード。

---

## 【フェーズ4：応用APIと品質・テスト要件】

### ステップ13：【応用】公開API（CRUD）の実装
* **ルーティング**: `/api/v1/contacts...`
* 認証不要の5つのエンドポイント（一覧・詳細・作成・更新・削除）の実装。
* 専用バリデーション（性別 `in:1,2,3`、`per_page` 可変）の定義。
* API Resource（meta情報含む）によるJSON返却と、404/422の日本語エラー制御。

### ステップ14：コード自動整形とテストコードの実行
* Laravel Pint（`sail bin pint --test`）でのエラーなし確認。
* 単体・機能テストの作成とカバレッジ70%超えの達成。